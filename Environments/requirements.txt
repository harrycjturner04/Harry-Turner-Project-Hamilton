# Installed via: uv pip install -r Environments/requirements.txt
# (uv handles the complex dependency graph that causes pip resolution-too-deep errors)
#
# vLLM is NOT listed here — it requires CUDA_HOME at build time and is installed
# by the batch script after 'module load cuda'.
#
# browser-use is excluded from ag2 extras — browser-use==0.1.37 has a broken
# dependency (requires langchain==0.3.14 which pins requests==2.28.1, but also
# requires requests>=2.32.3). Playwright handles browser automation instead.

ag2[openai,captainagent,crawl4ai]>=0.11
transformers>=4.46,<5
openai>=1.40
requests
python-dotenv
deepeval
sentence-transformers>=2.2.0
chromadb>=0.4.6
weasyprint>=60.0
markdown>=3.5
reportlab>=4.0
playwright>=1.40
protobuf>=3.20,<7
