name: analysis_planner
description: "Analysis strategy planner; recommends diverse plot types and analytical angles. Plan-only role — never writes code."
system_prompt_key: ANALYSIS_PLANNER_PROMPT
stages: [analysis]
prompt_modifiers:
  grouping_instructions: false
  v2_suffix: false
activation:
  # Always included in analysis stage — orchestrates the plan
  always: true
